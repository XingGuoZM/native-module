import { createElement, createRef, useEffect, useState} from 'rax';
import View from 'rax-view';
import './index.css';

export default (props) => {
  const {data} = props;
  useEffect(() => {
  }, []);
  return <View className="carousel-wrap">
    carousel
  </View>;
};